#if !defined(__UTILS__HEADER__)
#define __UTILS__HEADER__
#include <stdbool.h>
#include <stdio.h>
char* concat(char*, char*);
bool get_no(char*, int *);
FILE* asm_fopen(char* );
bool is_whitespace(char* );
#endif


